# {{HEADER_COMMENT}}
# Generated by gen-upgrade-config skill on {{GENERATED_AT}}
# Run: ./upgrade --config {{SELF_PATH}}
# Local mode: `make deploy` (or override via operatorConfig.command) is invoked
# in {{WORKSPACE}} instead of going through OLM/violet.

immediate: {{IMMEDIATE}}
logLevel: {{LOG_LEVEL}}

operatorConfig:
  type: local
  name: {{OPERATOR_NAME}}
  namespace: {{OPERATOR_NAMESPACE}}
  workspace: {{WORKSPACE}}
  # command: make deploy             # 默认 make deploy；自定义部署命令时改这里
  timeout: {{TIMEOUT}}
  interval: {{INTERVAL}}

upgradePaths:
  - name: {{PATH_NAME}}
    versions:
{{VERSIONS_BLOCK}}
